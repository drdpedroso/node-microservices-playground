version: "3"
services:
  node:
    build:
      dockerfile: Dockerfile
      context: .
    user: "node"
    working_dir: /home/node/app
    environment:
    - NODE_ENV=production
    volumes:
    - ./:/home/node/app
    ports:
    - "3000:3000"
    command: "npm start"
